<%= link_to boardgame_path(boardgame), class: "text-decoration-none" do %>
  <div class="card-trip" style="background-image: url('<%= cl_image_path(boardgame.image, height: 100, crop: :fit) %>')">
    <p class="game-name"><%= boardgame.title %></p>
  </div>
<% end %>
